git init : turn te folder to git repo  
git remote add origin<url> : links your folder to repo  
get remote -v : where is my file in the git repo  
git add . : adds the changes  
git commit -m "" : saves ur changes with a note  
git branch : shoes which branch you are currently working on  
git checkout <main> : goes to the existing branch  
git stash : hides the errors  
rm -rf git : completely removes git from the folder  
ls -a : show hidden files  
touch: make a new file  
git status: the things which are not in the history of the project  
cat : shows everthing present in the file  
git restore --staged : can remove the previous adds  
git log : whole history of the project  
git reset : removes previous commits  
git stash : not commited but not lost  
git stash pop; commit stash  
git | stash clear : clear stash  
